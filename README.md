# Jsonite

## Types

In Jsonite, (almost) everything is a `JsonValue`.

## Parsing JSON

```Java
    JsonValue value = Transformation.parse("{\"foo\": 1, \"bar\": false}");
```

## Building JSON

## Streaming
